# Timbuk2-Ai Tech Assessment

After cloning the project you will need to install the node_modules by:

`npm install` for those using npm or
`yarn` for those using yarn

Then simply run `yarn start or npm start`